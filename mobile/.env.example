# Base URL of the Construction ERP backend API (no trailing slash).
# For a physical device on the same Wi-Fi use your machine's LAN IP, e.g. http://192.168.1.10:4000
# For the Android emulator use http://10.0.2.2:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
